{
    "name": "anecdote-bot",
    "version": "2.0.0",
    "type": "module",
    "description": "Professional Telegram anecdote bot with Click payment integration",
    "scripts": {
        "dev": "tsx watch src/main.ts",
        "start": "tsx src/bot-simple.ts",
        "start:full": "tsx src/main.ts",
        "build": "tsc",
        "start:prod": "node --env-file=.env dist/main.js",
        "start:gateway": "node --env-file=.env dist/gateway.js",
        "sync": "tsx src/scripts/sync-anecdotes.ts",
        "migration:generate": "typeorm-ts-node-esm migration:generate",
        "migration:create": "typeorm-ts-node-esm migration:create",
        "migration:run": "npm run build && node --env-file=.env -e \"require('dotenv').config(); require('./dist/database/data-source.js').AppDataSource.initialize().then(ds => ds.runMigrations()).then(() => process.exit(0)).catch(e => {console.error(e); process.exit(1);})\"",
        "migration:revert": "npm run build && node --env-file=.env -e \"require('dotenv').config(); require('./dist/database/data-source.js').AppDataSource.initialize().then(ds => ds.undoLastMigration()).then(() => process.exit(0)).catch(e => {console.error(e); process.exit(1);})\""
    },
    "dependencies": {
        "axios": "^1.13.2",
        "body-parser": "^1.20.2",
        "canvas": "^3.2.0",
        "dotenv": "^16.0.0",
        "express": "^4.18.2",
        "grammy": "^1.38.4",
        "pg": "^8.11.0",
        "reflect-metadata": "^0.1.13",
        "tsx": "^4.21.0",
        "typeorm": "^0.3.17",
        "uuid": "^9.0.0"
    },
    "devDependencies": {
        "@types/body-parser": "^1.19.6",
        "@types/express": "^5.0.6",
        "@types/node": "^25.0.1",
        "ts-node": "^10.9.1",
        "typescript": "^5.4.2"
    }
}